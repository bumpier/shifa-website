# Admin Subusers + Analytics Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add role-based subuser accounts (Admin/Packer) and an analytics dashboard with revenue charts, order stats, top products, traffic sources, and payment method breakdowns.

**Architecture:** New `AdminUser` Prisma model stores Packer accounts (email + bcrypt password). The existing env-var super-admin stays as a fallback. JWT sessions gain a `role` field; middleware enforces Packers to `/admin/orders/*` only. Analytics dashboard is server-fetched + client-rendered with Recharts, Admin-only. A new "packed" order status is added between "paid" and "shipped".

**Tech Stack:** Next.js App Router, Prisma (SQLite), bcryptjs, jose (JWT), Recharts, Zod

---

## File Map

**New files:**
- `prisma/migrations/[auto]/` — auto-generated by `prisma migrate dev`
- `app/admin/subusers/page.tsx` — list + create + toggle AdminUsers (Admin only)
- `app/admin/subusers/actions.ts` — server actions: create/toggle AdminUser
- `app/admin/analytics/page.tsx` — server component: fetches data, renders `AnalyticsDashboard`
- `components/admin/AnalyticsDashboard.tsx` — client component with Recharts charts

**Modified files:**
- `prisma/schema.prisma` — add `AdminUser` model
- `lib/adminAuth.ts` — add `createAdminUserSession`, `getAdminSession`, `requireAdminRole`
- `middleware.ts` — role-based route protection (PACKER → /admin/orders only)
- `app/admin/actions.ts` — update login action, add `requireAdminRole` to admin-only actions
- `app/admin/login/page.tsx` — add email field for subuser login
- `app/admin/layout.tsx` — role-aware nav (hide Analytics/Team/Products/Affiliates from Packer)
- `app/admin/orders/page.tsx` — add "packed" status, hide Total column for Packer, add quick-action buttons

---

## Task 1: AdminUser Prisma Model

**Files:**
- Modify: `prisma/schema.prisma`

- [ ] **Step 1: Add AdminUser model to schema**

Open `prisma/schema.prisma` and append after the existing models:

```prisma
model AdminUser {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  name         String
  role         String   @default("PACKER") // ADMIN | PACKER
  active       Boolean  @default(true)
  createdAt    DateTime @default(now())
}
```

- [ ] **Step 2: Run migration**

```bash
cd /Users/liam/development/shifa-website
npx prisma migrate dev --name add_admin_users
```

Expected output: `✔  Generated Prisma Client` and a new file under `prisma/migrations/`.

- [ ] **Step 3: Verify Prisma client generated correctly**

```bash
npx prisma studio
```

Confirm `AdminUser` table is visible. Close Studio (Ctrl+C). Alternatively:

```bash
node -e "const {PrismaClient}=require('@prisma/client');const p=new PrismaClient();p.adminUser.count().then(n=>console.log('AdminUser count:',n)).finally(()=>p.\$disconnect())"
```

Expected: `AdminUser count: 0`

- [ ] **Step 4: Commit**

```bash
git add prisma/schema.prisma prisma/migrations/
git commit -m "feat: add AdminUser model for role-based subusers"
```

---

## Task 2: Extend adminAuth.ts

**Files:**
- Modify: `lib/adminAuth.ts`

- [ ] **Step 1: Add new exports to `lib/adminAuth.ts`**

The file currently exports: `verifyAdminPassword`, `createAdminSession`, `destroyAdminSession`, `isAdminToken`, `requireAdmin`, `isAdmin`.

Add these exports at the bottom of the file (keep all existing code untouched):

```ts
export type AdminRole = "ADMIN" | "PACKER";

/** Create a session for an AdminUser (stores role + id in JWT). */
export async function createAdminUserSession(adminUser: {
  id: string;
  role: string;
}): Promise<void> {
  const role: AdminRole = adminUser.role === "PACKER" ? "PACKER" : "ADMIN";
  const token = await new SignJWT({ admin: true, role, adminUserId: adminUser.id })
    .setProtectedHeader({ alg: "HS256" })
    .setIssuedAt()
    .setExpirationTime(`${ADMIN_SESSION_HOURS}h`)
    .sign(secret());

  (await cookies()).set(ADMIN_COOKIE, token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === "production",
    sameSite: "strict",
    maxAge: ADMIN_SESSION_HOURS * 3600,
    path: "/",
  });
}

/** Returns session info from cookie. Legacy env-var tokens (no role field) are treated as ADMIN. */
export async function getAdminSession(): Promise<{
  role: AdminRole;
  adminUserId?: string;
} | null> {
  const token = (await cookies()).get(ADMIN_COOKIE)?.value;
  if (!token) return null;
  try {
    const { payload } = await jwtVerify(token, secret());
    if (payload.admin !== true) return null;
    const role: AdminRole =
      (payload.role as string) === "PACKER" ? "PACKER" : "ADMIN";
    return {
      role,
      adminUserId: payload.adminUserId as string | undefined,
    };
  } catch {
    return null;
  }
}

/**
 * Throws if the current session is not authenticated OR if `role` is 'ADMIN'
 * and the session belongs to a Packer.
 * Use requireAdmin() for packer-accessible routes (it only checks admin===true).
 * Use requireAdminRole('ADMIN') for Admin-only routes.
 */
export async function requireAdminRole(role: AdminRole = "ADMIN"): Promise<void> {
  const session = await getAdminSession();
  if (!session) throw new Error("Unauthorised");
  if (role === "ADMIN" && session.role !== "ADMIN") throw new Error("Unauthorised");
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
cd /Users/liam/development/shifa-website
npx tsc --noEmit 2>&1 | head -30
```

Expected: no errors related to `lib/adminAuth.ts`.

- [ ] **Step 3: Commit**

```bash
git add lib/adminAuth.ts
git commit -m "feat: add role-aware session functions to adminAuth"
```

---

## Task 3: Update Login Page + Action

**Files:**
- Modify: `app/admin/login/page.tsx`
- Modify: `app/admin/actions.ts`

- [ ] **Step 1: Add email field to login page**

Replace the entire contents of `app/admin/login/page.tsx` with:

```tsx
"use client";

import { useActionState } from "react";
import { adminLoginAction } from "@/app/admin/actions";
import type { FormState } from "@/app/(store)/auth/actions";
import { FormMessage, SubmitButton } from "@/components/forms";

const initial: FormState = {};

export default function AdminLoginPage() {
  const [state, action] = useActionState(adminLoginAction, initial);

  return (
    <div className="mx-auto max-w-sm px-4 py-24 sm:px-6">
      <div className="card p-8">
        <p className="eyebrow">Restricted area</p>
        <h1 className="mt-2 font-display text-2xl font-medium tracking-tight text-brand-deep">
          Admin sign in
        </h1>

        <form action={action} className="mt-7 space-y-5">
          <div>
            <label className="label" htmlFor="email">
              Email{" "}
              <span className="font-normal text-ink-soft">(leave blank for master password)</span>
            </label>
            <input
              id="email"
              name="email"
              type="email"
              maxLength={254}
              className="field"
              autoComplete="email"
            />
          </div>
          <div>
            <label className="label" htmlFor="password">
              Password
            </label>
            <input
              id="password"
              name="password"
              type="password"
              required
              maxLength={256}
              className="field"
              autoComplete="current-password"
            />
          </div>

          <FormMessage state={state} />
          <SubmitButton>Sign in</SubmitButton>
        </form>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Update `adminLoginAction` in `app/admin/actions.ts`**

At the top of `app/admin/actions.ts`, add two new imports after the existing imports:

```ts
import { prisma } from "@/lib/db";
import bcrypt from "bcryptjs";
import {
  createAdminSession,
  createAdminUserSession,
  destroyAdminSession,
  requireAdmin,
  requireAdminRole,
  verifyAdminPassword,
} from "@/lib/adminAuth";
```

Note: `prisma` is already imported in this file. `bcrypt` is NOT — add `import bcrypt from "bcryptjs"` as a new import line. Add `createAdminUserSession` and `requireAdminRole` to the existing `adminAuth` import.

Replace the entire `adminLoginAction` function with:

```ts
export async function adminLoginAction(
  _prev: FormState,
  formData: FormData
): Promise<FormState> {
  const key = `admin-login:${await ip()}`;
  if (isLockedOut(key)) {
    return { error: "Too many failed attempts. Try again in 15 minutes." };
  }
  if (!rateLimit(`${key}:req`, 10, 60_000)) {
    return { error: "Too many attempts. Please wait a minute." };
  }

  const emailRaw = formData.get("email");
  const password = formData.get("password");
  if (typeof password !== "string" || password.length === 0 || password.length > 256) {
    recordFailure(key);
    return { error: "Invalid credentials" };
  }

  // AdminUser login (email provided)
  if (typeof emailRaw === "string" && emailRaw.trim()) {
    const email = emailRaw.trim().toLowerCase();
    const adminUser = await prisma.adminUser.findUnique({ where: { email } });
    if (
      !adminUser ||
      !adminUser.active ||
      !(await bcrypt.compare(password, adminUser.passwordHash))
    ) {
      recordFailure(key);
      return { error: "Invalid credentials" };
    }
    clearFailures(key);
    await createAdminUserSession(adminUser);
    redirect(adminUser.role === "PACKER" ? "/admin/orders" : "/admin");
  }

  // Env-var super-admin fallback (no email)
  if (!(await verifyAdminPassword(password))) {
    recordFailure(key);
    return { error: "Invalid credentials" };
  }
  clearFailures(key);
  await createAdminSession();
  redirect("/admin");
}
```

- [ ] **Step 3: Add `requireAdminRole` to all admin-only actions**

In `app/admin/actions.ts`, replace `await requireAdmin()` with `await requireAdminRole("ADMIN")` in these functions: `saveProductAction`, `deleteProductAction`, `createInviteAction`, `setCommissionRateAction`, `setAffiliateStatusAction`, `reviewReferralAction`, `processPayoutAction`.

Leave `setOrderStatusAction` using `await requireAdmin()` — packers can update order status.

- [ ] **Step 4: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: no errors.

- [ ] **Step 5: Commit**

```bash
git add app/admin/login/page.tsx app/admin/actions.ts
git commit -m "feat: add email+password login for AdminUser subusers"
```

---

## Task 4: Update Middleware for Role-Based Protection

**Files:**
- Modify: `middleware.ts`

- [ ] **Step 1: Replace the admin protection block in `middleware.ts`**

The current admin block (lines ~39–62) checks `validAdminToken`. Replace the entire admin protection block with role-aware logic. Replace from `// ── Admin protection` to the closing `return res;` of that block:

```ts
// ── Admin protection (everything under /admin except the login page)
if (pathname.startsWith("/admin") && pathname !== "/admin/login") {
  const token = req.cookies.get("admin_session")?.value;
  const role = await getAdminTokenRole(token);

  if (!role) {
    const url = req.nextUrl.clone();
    url.pathname = "/admin/login";
    url.search = "";
    return NextResponse.redirect(url);
  }

  // Packers are restricted to /admin/orders and /admin/orders/*
  if (role === "PACKER" && !pathname.startsWith("/admin/orders")) {
    const url = req.nextUrl.clone();
    url.pathname = "/admin/orders";
    url.search = "";
    return NextResponse.redirect(url);
  }

  // Sliding expiry: re-issue token preserving role
  const res = NextResponse.next();
  const refreshPayload: Record<string, unknown> = { admin: true };
  if (role === "PACKER") refreshPayload.role = "PACKER";
  const refreshed = await new SignJWT(refreshPayload)
    .setProtectedHeader({ alg: "HS256" })
    .setIssuedAt()
    .setExpirationTime(`${ADMIN_SESSION_HOURS}h`)
    .sign(secret());
  res.cookies.set("admin_session", refreshed, {
    httpOnly: true,
    secure: process.env.NODE_ENV === "production",
    sameSite: "strict",
    maxAge: ADMIN_SESSION_HOURS * 3600,
    path: "/",
  });
  return res;
}
```

- [ ] **Step 2: Add `getAdminTokenRole` helper function to `middleware.ts`**

Add this function alongside the existing `validAdminToken` and `validUserToken` helpers (before the `middleware` export):

```ts
async function getAdminTokenRole(
  token: string | undefined
): Promise<"ADMIN" | "PACKER" | null> {
  if (!token) return null;
  try {
    const { payload } = await jwtVerify(token, secret());
    if (payload.admin !== true) return null;
    return (payload.role as string) === "PACKER" ? "PACKER" : "ADMIN";
  } catch {
    return null;
  }
}
```

You can remove the existing `validAdminToken` function since it's replaced by `getAdminTokenRole`.

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add middleware.ts
git commit -m "feat: enforce role-based route protection in middleware"
```

---

## Task 5: Role-Aware Admin Layout + Nav

**Files:**
- Modify: `app/admin/layout.tsx`
- Modify: `app/admin/page.tsx`

- [ ] **Step 1: Update `app/admin/layout.tsx` to show role-filtered nav**

Replace the entire file:

```tsx
import Link from "next/link";
import { brand } from "@/config/brand";
import { getAdminSession } from "@/lib/adminAuth";
import { adminLogoutAction } from "@/app/admin/actions";

const ALL_NAV = [
  { href: "/admin", label: "Overview", adminOnly: true },
  { href: "/admin/orders", label: "Orders", adminOnly: false },
  { href: "/admin/products", label: "Products", adminOnly: true },
  { href: "/admin/affiliates", label: "Affiliates", adminOnly: true },
  { href: "/admin/analytics", label: "Analytics", adminOnly: true },
  { href: "/admin/subusers", label: "Team", adminOnly: true },
];

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const session = await getAdminSession();
  const isAdminRole = session?.role === "ADMIN";

  const nav = ALL_NAV.filter((item) => !item.adminOnly || isAdminRole);

  return (
    <div className="relative z-10 flex min-h-screen flex-col">
      <header className="no-print border-b border-line bg-brand-deep text-white">
        <div className="mx-auto flex h-14 max-w-6xl items-center justify-between gap-4 px-4 sm:px-6">
          <div className="flex items-center gap-6">
            <Link href={isAdminRole ? "/admin" : "/admin/orders"} className="flex items-center gap-2">
              <span className="font-display text-lg font-semibold">{brand.name}</span>
              <span className="rounded-full bg-white/15 px-2 py-0.5 text-[10px] font-bold uppercase tracking-wider">
                {session?.role === "PACKER" ? "Packer" : "Admin"}
              </span>
            </Link>
            {session && (
              <nav className="flex items-center gap-4 text-sm">
                {nav.map((item) => (
                  <Link
                    key={item.href}
                    href={item.href}
                    className="text-white/75 transition-colors hover:text-white"
                  >
                    {item.label}
                  </Link>
                ))}
              </nav>
            )}
          </div>
          {session && (
            <form action={adminLogoutAction}>
              <button type="submit" className="text-sm text-white/75 hover:text-white">
                Sign out
              </button>
            </form>
          )}
        </div>
      </header>
      <main className="flex-1">{children}</main>
    </div>
  );
}
```

- [ ] **Step 2: Add packer redirect to `app/admin/page.tsx`**

Add this at the top of `AdminOverviewPage`, right after `await requireAdmin()`:

```ts
import { getAdminSession } from "@/lib/adminAuth";
import { redirect } from "next/navigation";

// Inside the component, after await requireAdmin():
const session = await getAdminSession();
if (session?.role === "PACKER") redirect("/admin/orders");
```

Full beginning of the function should look like:
```ts
export default async function AdminOverviewPage() {
  await requireAdmin();
  const session = await getAdminSession();
  if (session?.role === "PACKER") redirect("/admin/orders");
  // ...rest unchanged
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/layout.tsx app/admin/page.tsx
git commit -m "feat: role-aware nav and packer redirect on admin overview"
```

---

## Task 6: Add "Packed" Order Status

**Files:**
- Modify: `app/admin/actions.ts`
- Modify: `app/admin/orders/page.tsx`

- [ ] **Step 1: Add "packed" to the order status schema in `app/admin/actions.ts`**

Find:
```ts
const orderStatusSchema = z.enum(["pending", "paid", "shipped", "delivered", "cancelled"]);
```

Replace with:
```ts
const orderStatusSchema = z.enum(["pending", "paid", "packed", "shipped", "delivered", "cancelled"]);
```

- [ ] **Step 2: Update `app/admin/orders/page.tsx` — add "packed" status + role-aware columns**

Replace the entire file:

```tsx
import Link from "next/link";
import { prisma } from "@/lib/db";
import { requireAdmin, getAdminSession } from "@/lib/adminAuth";
import { formatPrice, type Currency } from "@/config/brand";
import { setOrderStatusAction } from "@/app/admin/actions";

export const dynamic = "force-dynamic";

const STATUSES = ["all", "pending", "paid", "packed", "shipped", "delivered", "cancelled"] as const;

const STATUS_STYLES: Record<string, string> = {
  pending: "bg-amber-50 text-amber-700",
  paid: "bg-brand-tint text-brand-deep",
  packed: "bg-blue-50 text-blue-700",
  shipped: "bg-indigo-50 text-indigo-700",
  delivered: "bg-brand text-white",
  cancelled: "bg-red-50 text-red-600",
};

// What status a packer can advance an order to
const PACKER_NEXT_STATUS: Record<string, string | null> = {
  paid: "packed",
  packed: "shipped",
};

export default async function AdminOrdersPage({
  searchParams,
}: {
  searchParams: Promise<{ status?: string }>;
}) {
  await requireAdmin();
  const session = await getAdminSession();
  const isPacker = session?.role === "PACKER";

  const { status } = await searchParams;
  const filter = STATUSES.includes(status as never) ? status : "all";

  const orders = await prisma.order.findMany({
    where: filter && filter !== "all" ? { status: filter } : undefined,
    orderBy: { createdAt: "desc" },
    take: 200,
  });

  return (
    <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6">
      <p className="eyebrow">Fulfilment</p>
      <h1 className="mt-2 font-display text-3xl font-medium tracking-tight text-brand-deep">
        Orders
      </h1>

      <div className="mt-8 flex flex-wrap gap-2">
        {STATUSES.map((s) => (
          <Link
            key={s}
            href={s === "all" ? "/admin/orders" : `/admin/orders?status=${s}`}
            className={`rounded-full px-4 py-1.5 text-sm font-medium capitalize transition-colors ${
              filter === s
                ? "bg-brand text-white"
                : "border border-line bg-white text-ink-soft hover:border-brand"
            }`}
          >
            {s}
          </Link>
        ))}
      </div>

      {orders.length === 0 ? (
        <p className="card mt-8 p-8 text-center text-sm text-ink-soft">No orders found.</p>
      ) : (
        <div className="card mt-8 overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-line text-left text-xs uppercase tracking-wider text-ink-soft">
                <th className="px-5 py-3 font-semibold">Date</th>
                <th className="px-5 py-3 font-semibold">Customer</th>
                {!isPacker && <th className="px-5 py-3 font-semibold">Total</th>}
                <th className="px-5 py-3 font-semibold">Method</th>
                <th className="px-5 py-3 font-semibold">Status</th>
                <th className="px-5 py-3" />
              </tr>
            </thead>
            <tbody className="divide-y divide-line">
              {orders.map((o) => {
                const nextStatus = PACKER_NEXT_STATUS[o.status];
                return (
                  <tr key={o.id} className="hover:bg-brand-tint/40">
                    <td className="px-5 py-3 text-ink-soft">
                      {o.createdAt.toLocaleDateString("en-GB", {
                        day: "2-digit",
                        month: "short",
                      })}
                    </td>
                    <td className="px-5 py-3">
                      <span className="font-medium">{o.customerName}</span>
                      <span className="block text-xs text-ink-soft">{o.customerEmail}</span>
                    </td>
                    {!isPacker && (
                      <td className="px-5 py-3 font-medium">
                        {formatPrice(o.totalAmount.toString(), o.currency as Currency)}
                      </td>
                    )}
                    <td className="px-5 py-3 capitalize text-ink-soft">{o.paymentMethod}</td>
                    <td className="px-5 py-3">
                      <span
                        className={`rounded-full px-2.5 py-1 text-xs font-semibold ${
                          STATUS_STYLES[o.status] ?? ""
                        }`}
                      >
                        {o.status}
                      </span>
                    </td>
                    <td className="px-5 py-3 text-right">
                      <div className="flex items-center justify-end gap-3">
                        {isPacker && nextStatus && (
                          <form action={setOrderStatusAction}>
                            <input type="hidden" name="orderId" value={o.id} />
                            <input type="hidden" name="status" value={nextStatus} />
                            <button
                              type="submit"
                              className="rounded-full bg-brand px-3 py-1 text-xs font-semibold text-white hover:bg-brand-deep"
                            >
                              Mark {nextStatus} →
                            </button>
                          </form>
                        )}
                        <Link
                          href={`/admin/orders/${o.id}`}
                          className="text-sm font-semibold text-brand hover:text-brand-deep"
                        >
                          View →
                        </Link>
                      </div>
                    </td>
                  </tr>
                );
              })}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/actions.ts app/admin/orders/page.tsx
git commit -m "feat: add packed status and packer quick-action buttons in orders"
```

---

## Task 7: Subuser Management Page

**Files:**
- Create: `app/admin/subusers/page.tsx`
- Create: `app/admin/subusers/actions.ts`

- [ ] **Step 1: Create `app/admin/subusers/actions.ts`**

```ts
"use server";

import bcrypt from "bcryptjs";
import { revalidatePath } from "next/cache";
import { z } from "zod";
import { prisma } from "@/lib/db";
import { requireAdminRole } from "@/lib/adminAuth";
import type { FormState } from "@/app/(store)/auth/actions";

const CreateSubuserSchema = z.object({
  name: z.string().min(1).max(100),
  email: z.string().email().max(254).transform((v) => v.toLowerCase()),
  password: z.string().min(8).max(128),
});

export async function createSubuserAction(
  _prev: FormState,
  formData: FormData
): Promise<FormState> {
  await requireAdminRole("ADMIN");

  const parsed = CreateSubuserSchema.safeParse({
    name: formData.get("name"),
    email: formData.get("email"),
    password: formData.get("password"),
  });
  if (!parsed.success) {
    return { error: parsed.error.errors[0]?.message ?? "Invalid details" };
  }

  const existing = await prisma.adminUser.findUnique({
    where: { email: parsed.data.email },
  });
  if (existing) return { error: "An account with that email already exists" };

  const passwordHash = await bcrypt.hash(parsed.data.password, 12);
  await prisma.adminUser.create({
    data: {
      name: parsed.data.name,
      email: parsed.data.email,
      passwordHash,
      role: "PACKER",
    },
  });

  revalidatePath("/admin/subusers");
  return { success: `Account created for ${parsed.data.email}` };
}

export async function toggleSubuserAction(formData: FormData): Promise<void> {
  await requireAdminRole("ADMIN");
  const id = z.string().cuid().parse(formData.get("id"));
  const active = formData.get("active") === "true";
  await prisma.adminUser.update({ where: { id }, data: { active: !active } });
  revalidatePath("/admin/subusers");
}
```

- [ ] **Step 2: Create `app/admin/subusers/page.tsx`**

This is a Server Component — do NOT add `"use client"` at the top. Create the file:

```tsx
import { requireAdminRole } from "@/lib/adminAuth";
import { prisma } from "@/lib/db";
import {
  createSubuserAction,
  toggleSubuserAction,
} from "@/app/admin/subusers/actions";
import { FormMessage, SubmitButton } from "@/components/forms";
import SubuserForm from "./SubuserForm";

export const dynamic = "force-dynamic";

export default async function SubusersPage() {
  await requireAdminRole("ADMIN");

  const users = await prisma.adminUser.findMany({
    orderBy: { createdAt: "desc" },
  });

  return (
    <div className="mx-auto max-w-4xl px-4 py-12 sm:px-6">
      <p className="eyebrow">Access control</p>
      <h1 className="mt-2 font-display text-3xl font-medium tracking-tight text-brand-deep">
        Team members
      </h1>

      {/* Existing subusers */}
      {users.length > 0 && (
        <div className="card mt-10 overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-line text-left text-xs uppercase tracking-wider text-ink-soft">
                <th className="px-5 py-3 font-semibold">Name</th>
                <th className="px-5 py-3 font-semibold">Email</th>
                <th className="px-5 py-3 font-semibold">Role</th>
                <th className="px-5 py-3 font-semibold">Status</th>
                <th className="px-5 py-3" />
              </tr>
            </thead>
            <tbody className="divide-y divide-line">
              {users.map((u) => (
                <tr key={u.id} className="hover:bg-brand-tint/40">
                  <td className="px-5 py-3 font-medium">{u.name}</td>
                  <td className="px-5 py-3 text-ink-soft">{u.email}</td>
                  <td className="px-5 py-3 capitalize text-ink-soft">
                    {u.role.toLowerCase()}
                  </td>
                  <td className="px-5 py-3">
                    <span
                      className={`rounded-full px-2.5 py-1 text-xs font-semibold ${
                        u.active
                          ? "bg-brand-tint text-brand-deep"
                          : "bg-red-50 text-red-600"
                      }`}
                    >
                      {u.active ? "Active" : "Suspended"}
                    </span>
                  </td>
                  <td className="px-5 py-3 text-right">
                    <form action={toggleSubuserAction}>
                      <input type="hidden" name="id" value={u.id} />
                      <input
                        type="hidden"
                        name="active"
                        value={String(u.active)}
                      />
                      <button
                        type="submit"
                        className="text-sm font-semibold text-ink-soft hover:text-brand"
                      >
                        {u.active ? "Suspend" : "Reinstate"}
                      </button>
                    </form>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}

      {/* Create new subuser */}
      <div className="card mt-10 p-6">
        <h2 className="font-display text-xl font-medium text-brand-deep">
          Add packer account
        </h2>
        <SubuserForm />
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create `app/admin/subusers/SubuserForm.tsx` (client component for form state)**

```tsx
"use client";

import { useActionState } from "react";
import { createSubuserAction } from "@/app/admin/subusers/actions";
import type { FormState } from "@/app/(store)/auth/actions";
import { FormMessage, SubmitButton } from "@/components/forms";

const initial: FormState = {};

export default function SubuserForm() {
  const [state, action] = useActionState(createSubuserAction, initial);

  return (
    <form action={action} className="mt-5 space-y-5 max-w-md">
      <div>
        <label className="label" htmlFor="su-name">
          Full name
        </label>
        <input
          id="su-name"
          name="name"
          type="text"
          required
          maxLength={100}
          className="field"
        />
      </div>
      <div>
        <label className="label" htmlFor="su-email">
          Email
        </label>
        <input
          id="su-email"
          name="email"
          type="email"
          required
          maxLength={254}
          className="field"
        />
      </div>
      <div>
        <label className="label" htmlFor="su-password">
          Temporary password
        </label>
        <input
          id="su-password"
          name="password"
          type="password"
          required
          minLength={8}
          maxLength={128}
          className="field"
        />
      </div>
      <FormMessage state={state} />
      <SubmitButton>Create packer account</SubmitButton>
    </form>
  );
}
```

- [ ] **Step 4: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 5: Commit**

```bash
git add app/admin/subusers/
git commit -m "feat: add subuser management page for creating packer accounts"
```

---

## Task 8: Install Recharts + Build Analytics Data Layer

**Files:**
- Create: `components/admin/AnalyticsDashboard.tsx`

- [ ] **Step 1: Install Recharts**

```bash
cd /Users/liam/development/shifa-website
npm install recharts
```

Expected: `added N packages`

- [ ] **Step 2: Create `components/admin/AnalyticsDashboard.tsx`**

This is a `"use client"` component that receives pre-computed data as props and renders all charts:

```tsx
"use client";

import {
  AreaChart,
  Area,
  BarChart,
  Bar,
  PieChart,
  Pie,
  Cell,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
  Legend,
} from "recharts";

const COLORS = ["#121271", "#3ec7ed", "#6366f1", "#10b981", "#f59e0b"];

export interface DailyRevenue {
  date: string; // "DD MMM"
  revenue: number;
}

export interface StatusCount {
  status: string;
  count: number;
}

export interface ProductCount {
  name: string;
  count: number;
}

export interface TrafficSource {
  name: string;
  value: number;
}

export interface PaymentMethod {
  name: string;
  value: number;
}

interface Props {
  dailyRevenue: DailyRevenue[];
  statusCounts: StatusCount[];
  topProducts: ProductCount[];
  trafficSources: TrafficSource[];
  paymentMethods: PaymentMethod[];
  totalRevenueAed: number;
  totalOrders: number;
}

export default function AnalyticsDashboard({
  dailyRevenue,
  statusCounts,
  topProducts,
  trafficSources,
  paymentMethods,
  totalRevenueAed,
  totalOrders,
}: Props) {
  return (
    <div className="space-y-8">
      {/* KPI row */}
      <div className="grid grid-cols-2 gap-4 lg:grid-cols-4">
        <div className="card p-5">
          <p className="text-xs font-semibold uppercase tracking-wider text-ink-soft">
            Total Revenue
          </p>
          <p className="mt-2 font-display text-3xl font-medium text-brand-deep">
            AED {totalRevenueAed.toLocaleString("en-US", { maximumFractionDigits: 0 })}
          </p>
        </div>
        <div className="card p-5">
          <p className="text-xs font-semibold uppercase tracking-wider text-ink-soft">
            Total Orders
          </p>
          <p className="mt-2 font-display text-3xl font-medium text-brand-deep">
            {totalOrders}
          </p>
        </div>
        {statusCounts
          .filter((s) => s.status === "paid" || s.status === "shipped")
          .map((s) => (
            <div key={s.status} className="card p-5">
              <p className="text-xs font-semibold uppercase tracking-wider text-ink-soft capitalize">
                {s.status}
              </p>
              <p className="mt-2 font-display text-3xl font-medium text-brand-deep">
                {s.count}
              </p>
            </div>
          ))}
      </div>

      {/* Main charts row */}
      <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
        {/* Revenue chart — takes 2 cols */}
        <div className="card p-6 lg:col-span-2">
          <h2 className="font-display text-lg font-medium text-brand-deep">
            Revenue — last 30 days (AED)
          </h2>
          <div className="mt-4 h-64">
            <ResponsiveContainer width="100%" height="100%">
              <AreaChart data={dailyRevenue}>
                <defs>
                  <linearGradient id="revenueGrad" x1="0" y1="0" x2="0" y2="1">
                    <stop offset="5%" stopColor="#121271" stopOpacity={0.3} />
                    <stop offset="95%" stopColor="#121271" stopOpacity={0} />
                  </linearGradient>
                </defs>
                <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
                <XAxis dataKey="date" tick={{ fontSize: 11 }} />
                <YAxis tick={{ fontSize: 11 }} />
                <Tooltip formatter={(v: number) => [`AED ${v.toFixed(0)}`, "Revenue"]} />
                <Area
                  type="monotone"
                  dataKey="revenue"
                  stroke="#121271"
                  strokeWidth={2}
                  fill="url(#revenueGrad)"
                />
              </AreaChart>
            </ResponsiveContainer>
          </div>
        </div>

        {/* Orders by status */}
        <div className="card p-6">
          <h2 className="font-display text-lg font-medium text-brand-deep">
            Orders by status
          </h2>
          <div className="mt-4 h-64">
            <ResponsiveContainer width="100%" height="100%">
              <BarChart data={statusCounts} layout="vertical">
                <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
                <XAxis type="number" tick={{ fontSize: 11 }} />
                <YAxis dataKey="status" type="category" tick={{ fontSize: 11 }} width={65} />
                <Tooltip />
                <Bar dataKey="count" fill="#121271" radius={[0, 4, 4, 0]} />
              </BarChart>
            </ResponsiveContainer>
          </div>
        </div>
      </div>

      {/* Bottom row */}
      <div className="grid grid-cols-1 gap-6 lg:grid-cols-3">
        {/* Top products */}
        <div className="card p-6 lg:col-span-1">
          <h2 className="font-display text-lg font-medium text-brand-deep">
            Top products (units)
          </h2>
          <div className="mt-4 h-48">
            <ResponsiveContainer width="100%" height="100%">
              <BarChart data={topProducts} layout="vertical">
                <CartesianGrid strokeDasharray="3 3" stroke="#f0f0f0" />
                <XAxis type="number" tick={{ fontSize: 11 }} />
                <YAxis dataKey="name" type="category" tick={{ fontSize: 10 }} width={90} />
                <Tooltip />
                <Bar dataKey="count" fill="#3ec7ed" radius={[0, 4, 4, 0]} />
              </BarChart>
            </ResponsiveContainer>
          </div>
        </div>

        {/* Traffic sources */}
        <div className="card p-6">
          <h2 className="font-display text-lg font-medium text-brand-deep">
            Traffic sources
          </h2>
          <div className="mt-4 h-48">
            <ResponsiveContainer width="100%" height="100%">
              <PieChart>
                <Pie
                  data={trafficSources}
                  dataKey="value"
                  nameKey="name"
                  cx="50%"
                  cy="50%"
                  outerRadius={70}
                  label={({ name, percent }) =>
                    `${name} ${(percent * 100).toFixed(0)}%`
                  }
                  labelLine={false}
                >
                  {trafficSources.map((_, i) => (
                    <Cell key={i} fill={COLORS[i % COLORS.length]} />
                  ))}
                </Pie>
                <Tooltip />
              </PieChart>
            </ResponsiveContainer>
          </div>
        </div>

        {/* Payment methods */}
        <div className="card p-6">
          <h2 className="font-display text-lg font-medium text-brand-deep">
            Payment methods
          </h2>
          <div className="mt-4 h-48">
            <ResponsiveContainer width="100%" height="100%">
              <PieChart>
                <Pie
                  data={paymentMethods}
                  dataKey="value"
                  nameKey="name"
                  cx="50%"
                  cy="50%"
                  outerRadius={70}
                  label={({ name, percent }) =>
                    `${name} ${(percent * 100).toFixed(0)}%`
                  }
                  labelLine={false}
                >
                  {paymentMethods.map((_, i) => (
                    <Cell key={i} fill={COLORS[i % COLORS.length]} />
                  ))}
                </Pie>
                <Tooltip />
              </PieChart>
            </ResponsiveContainer>
          </div>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

- [ ] **Step 4: Commit**

```bash
git add components/admin/AnalyticsDashboard.tsx package.json package-lock.json
git commit -m "feat: add AnalyticsDashboard client component with Recharts"
```

---

## Task 9: Analytics Page

**Files:**
- Create: `app/admin/analytics/page.tsx`

- [ ] **Step 1: Create `app/admin/analytics/page.tsx`**

```tsx
import { prisma } from "@/lib/db";
import { requireAdminRole } from "@/lib/adminAuth";
import { Prisma } from "@prisma/client";
import AnalyticsDashboard from "@/components/admin/AnalyticsDashboard";
import type {
  DailyRevenue,
  StatusCount,
  ProductCount,
  TrafficSource,
  PaymentMethod,
} from "@/components/admin/AnalyticsDashboard";

export const dynamic = "force-dynamic";

export default async function AnalyticsPage() {
  await requireAdminRole("ADMIN");

  const thirtyDaysAgo = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000);

  const [revenueOrders, allStatusOrders, allPaidOrders, trafficCounts, paymentGroups] =
    await Promise.all([
      // Daily revenue: paid/shipped/delivered orders in last 30 days
      prisma.order.findMany({
        where: {
          createdAt: { gte: thirtyDaysAgo },
          status: { in: ["paid", "packed", "shipped", "delivered"] },
        },
        select: { createdAt: true, subtotalAed: true },
      }),
      // All orders for status breakdown
      prisma.order.groupBy({ by: ["status"], _count: { id: true } }),
      // All paid+ orders for top products
      prisma.order.findMany({
        where: { status: { in: ["paid", "packed", "shipped", "delivered"] } },
        select: { items: true },
      }),
      // Traffic: affiliate (refCode set) vs direct
      Promise.all([
        prisma.order.count({ where: { refCode: { not: null } } }),
        prisma.order.count({ where: { refCode: null } }),
      ]),
      // Payment methods
      prisma.order.groupBy({ by: ["paymentMethod"], _count: { id: true } }),
    ]);

  // ── Daily revenue aggregation
  const revenueByDate: Record<string, number> = {};
  for (const o of revenueOrders) {
    const dateKey = o.createdAt.toISOString().split("T")[0]!;
    revenueByDate[dateKey] = (revenueByDate[dateKey] ?? 0) + Number(o.subtotalAed);
  }
  const dailyRevenue: DailyRevenue[] = [];
  for (let i = 29; i >= 0; i--) {
    const d = new Date(Date.now() - i * 24 * 60 * 60 * 1000);
    const key = d.toISOString().split("T")[0]!;
    const label = d.toLocaleDateString("en-GB", { day: "2-digit", month: "short" });
    dailyRevenue.push({ date: label, revenue: Math.round(revenueByDate[key] ?? 0) });
  }

  // ── Status counts
  const statusCounts: StatusCount[] = allStatusOrders.map((s) => ({
    status: s.status,
    count: s._count.id,
  }));

  // ── Top products
  const productCounts: Record<string, number> = {};
  for (const o of allPaidOrders) {
    try {
      const items = JSON.parse(o.items) as Array<{ name: string; qty: number }>;
      for (const item of items) {
        productCounts[item.name] = (productCounts[item.name] ?? 0) + (item.qty ?? 1);
      }
    } catch {
      // malformed items JSON — skip
    }
  }
  const topProducts: ProductCount[] = Object.entries(productCounts)
    .sort(([, a], [, b]) => b - a)
    .slice(0, 5)
    .map(([name, count]) => ({ name, count }));

  // ── Traffic sources
  const [affiliateCount, directCount] = trafficCounts;
  const trafficSources: TrafficSource[] = [
    { name: "Affiliate", value: affiliateCount },
    { name: "Direct", value: directCount },
  ].filter((s) => s.value > 0);

  // ── Payment methods
  const paymentMethods: PaymentMethod[] = paymentGroups.map((g) => ({
    name: g.paymentMethod,
    value: g._count.id,
  }));

  // ── Summary KPIs
  const totalRevenueAed = revenueOrders.reduce(
    (sum, o) => sum + Number(o.subtotalAed),
    0
  );
  const totalOrders = allStatusOrders.reduce((sum, s) => sum + s._count.id, 0);

  return (
    <div className="mx-auto max-w-6xl px-4 py-12 sm:px-6">
      <p className="eyebrow">Insights</p>
      <h1 className="mt-2 font-display text-3xl font-medium tracking-tight text-brand-deep">
        Analytics
      </h1>
      <p className="mt-1 text-sm text-ink-soft">
        Revenue charts show the last 30 days. All other metrics are all-time.
      </p>

      <div className="mt-10">
        <AnalyticsDashboard
          dailyRevenue={dailyRevenue}
          statusCounts={statusCounts}
          topProducts={topProducts}
          trafficSources={trafficSources}
          paymentMethods={paymentMethods}
          totalRevenueAed={Math.round(totalRevenueAed)}
          totalOrders={totalOrders}
        />
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: no errors.

- [ ] **Step 3: Run dev server and manually verify**

```bash
npm run dev
```

Open `http://localhost:3000/admin/analytics` (log in first with the master password). Confirm:
- Page loads without errors
- Charts render (may show empty if no orders in DB — check for no JS errors in console)
- Nav shows "Analytics" link
- Nav shows "Team" link

Open `http://localhost:3000/admin/subusers`. Confirm form is visible.

- [ ] **Step 4: Commit**

```bash
git add app/admin/analytics/
git commit -m "feat: add analytics dashboard with revenue, orders, products, and traffic charts"
```

---

## Final Smoke Test

- [ ] **Log in with master password** (password only, no email) → lands on `/admin` ✓
- [ ] **Create a packer account** at `/admin/subusers` → success message shown ✓
- [ ] **Log in with packer email + password** → lands on `/admin/orders` ✓
- [ ] **Packer tries `/admin/products`** → redirected to `/admin/orders` ✓
- [ ] **Packer sees "Mark packed →" button** on paid orders ✓
- [ ] **Packer does NOT see Total column** or revenue data ✓
- [ ] **Packer nav shows only "Orders"** ✓
- [ ] **Admin visits `/admin/analytics`** → charts render ✓
- [ ] **Admin nav shows** Overview, Orders, Products, Affiliates, Analytics, Team ✓
- [ ] **Suspend packer at `/admin/subusers`** → login attempt returns "Invalid credentials" ✓
